# Northstar Workspace

A minimal, dependency-free frontend prepared for Alloy development sessions.

## Run with Docker

```sh
docker compose -f docker-compose.alloy.yaml up -d
```

The application listens on port `3000`. Alloy exposes it through the session preview at `http://localhost:8080`.
